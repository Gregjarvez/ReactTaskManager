import React, {Component} from "react";function CategoryItem(props) {    let d = props.data;    const name = props.name.toLowerCase();    const active = d.isActive.toLowerCase();     return (        <li             onClick={() => props.data.handleCatClick(props.name)}            className={(name === active ? "isCurrentlyActive" : "isNotActive")}        >            <span>{props.name}</span>            <span className="tasks">{props.data.categorySize(name)}</span>        </li>    )}class Categories extends Component {    constructor(props) {        super(props);    }    render() {        const nodeNames = this.props.data.nodeNames;        const list = nodeNames.map((nodename, i) => {            return (                <CategoryItem                    key={i}                    name={nodename}                    data={this.props.data}                />            )        });        return (            <div className="card--categories">                <h3 className="cat--title">Categories</h3>                <ul className="cat--parent">                    {list}                </ul>                <div className="cat--add">+</div>            </div>        )    }}export default Categories;